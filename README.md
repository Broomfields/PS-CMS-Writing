# PS-CMS-Writing
The most complex branch of the site's CMS. This repository acts as an orchestrator for multiple writing projects *(repos stored as submodules in the `Writing Projects` repo)*. It filters private planning data from public prose and utilizes CI/CD to transform Obsidian-based world-building notes into a structured Wiki format, while serving story PDFs as release artifacts.
